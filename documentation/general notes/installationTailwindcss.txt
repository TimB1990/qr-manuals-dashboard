# install 
npm install taildwindcss --save-dev

# generate config file
npx taildwind init

# sass -> app.scss

@tailwind base;

@tailwind components;

@tailwind utilities;

# laravel mix
mix.postCss('resources/css/main.css', 'public/css', [
  require('tailwindcss'),
])

# when using sass

const tailwindcss = require('tailwindcss')

mix.sass('resources/sass/app.scss', 'public/css')
  .options({
    processCssUrls: false,
    postCss: [ tailwindcss('./path/to/your/tailwind.config.js') ],
  })

# or:

npm install laravel-mix-taildwind --save-dev

then require(laravel-mix-tailwind)

# in web.php
Route::get('/', function (){
   return view('app')
})

## change welcome to app.blade.css

<link rel="stylesheet" type="text/css" href-"/css/app.css">

